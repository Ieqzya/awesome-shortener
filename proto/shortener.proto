syntax = "proto3";

package shortener;

option go_package = "awesome-shortener/internal/grpc/pb";

import "google/protobuf/empty.proto";

// ShortenerService предоставляет API для сокращения URL
service ShortenerService {
  // ShortenURL сокращает URL
  rpc ShortenURL (URLShortenRequest) returns (URLShortenResponse);
  
  // ExpandURL получает оригинальный URL по короткому ID
  rpc ExpandURL (URLExpandRequest) returns (URLExpandResponse);
  
  // ListUserURLs возвращает все URL пользователя
  rpc ListUserURLs (google.protobuf.Empty) returns (UserURLsResponse);
}

// URLShortenRequest запрос на сокращение URL
message URLShortenRequest {
  string url = 1;
}

// URLShortenResponse ответ с сокращенным URL
message URLShortenResponse {
  string result = 1;
}

// URLExpandRequest запрос на получение оригинального URL
message URLExpandRequest {
  string id = 1;
}

// URLExpandResponse ответ с оригинальным URL
message URLExpandResponse {
  string result = 1;
}

// UserURLsResponse список URL пользователя
message UserURLsResponse {
  repeated URLData url = 1;
}

// URLData данные об URL
message URLData {
  string short_url = 1;
  string original_url = 2;
}
